spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      label: master
      profile: dev
server:
  port: 1501
eureka:
  client:
    service-url:
      defaultZone: http://eureka.server.ip:8761/eureka/

